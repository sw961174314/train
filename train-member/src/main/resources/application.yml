server:
  port: 8001
  servlet:
    context-path: /member

spring:
  datasource:
    url: jdbc:mysql://localhost/train-member?useU-enicode=true&characterEncoding=utf-8&serverTimezone=GMT%2b8&useSSL=false&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: fa221d

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

logging:
  level:
    com:
      java:
        train:
          mapper: trace